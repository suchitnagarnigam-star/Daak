
services:
  backend:
    build:
      context: .
    container_name: mcl-ocr-backend

    environment:
      PADDLE_PDX_ENABLE_MKLDNN_BYDEFAULT: "0"

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app

    restart: unless-stopped